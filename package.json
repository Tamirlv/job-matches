{
  "name": "job-matcher",
  "version": "1.0.0",
  "private": true,
  "description": "Checks for new matching developer job postings every 3 hours and delivers them via HTML file and email.",
  "main": "src/run.js",
  "scripts": {
    "start": "node src/run.js",
    "test": "node --test"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.65.0",
    "dotenv": "^16.4.5",
    "nodemailer": "^10.0.1"
  }
}
